server:
  port: ${PORT:0}
spring:
  application:
    name: user-ws
  h2:
    console:
      settings:
        web-allow-others: true
      enabled: true
eureka:
  client:
    serviceUrl.defaultZone: http://localhost:8010/eureka
  instance.instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}


token.expiration_time: 86400
token.secret: teste
login.url.path: /users/login